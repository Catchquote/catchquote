// Stripe — configure VITE_STRIPE_PUBLISHABLE_KEY in .env
// import { loadStripe } from '@stripe/stripe-js'
//
// export const stripePromise = loadStripe(import.meta.env.VITE_STRIPE_PUBLISHABLE_KEY)
//
// Subscription plans:
// - Starter: 5 quotes/month  — price_starter
// - Pro:     Unlimited quotes — price_pro
// - Agency:  Multi-user + white-label — price_agency

export const stripePromise = null // placeholder until Stripe is configured
